import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

const CustomerPage = () => {
  return (
    <ThemedView style={{ backgroundColor: "yellow" }}>
      <ThemedText>page customer</ThemedText>
    </ThemedView>
  );
};
